// 默认状态值
const DEFAULT_STATUS = 'on';
const COLLECTION_NAME = 'deduction-services';

const CODE_MSG_MAP = {
  '-1': '服务异常',
};

module.exports = {
  DEFAULT_STATUS,
  COLLECTION_NAME,
  CODE_MSG_MAP,
};
